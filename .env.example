# Copia este archivo como .env.local para cambiar valores en tu equipo.
# Estas variables son públicas (terminan dentro del HTML): nunca pongas secretos.

# Dominio público del sitio, sin barra final. Se usa en canonical, Open Graph,
# datos estructurados, robots.txt y sitemap.xml. En Vercel se define en
# Project Settings → Environment Variables cuando haya dominio propio.
VITE_SITE_URL=https://tecnitextil.vercel.app
